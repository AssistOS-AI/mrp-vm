# DS009 — KB Indexing (BM25 Backend)

## Purpose
Defines the BM25 lexical backend reused by one or
more KB plugins, especially `kb-fast` and
`kb-balanced`.

## Architectural Position

BM25 is a backend component, not a user-visible KB
selection.

## Dependencies

- DS023 — KB plugins
